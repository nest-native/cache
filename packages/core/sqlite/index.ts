// @stalefree/core/sqlite — the better-sqlite3 (synchronous) L2 store + table
// factory. Same-machine cross-process coherence comes from the socket bus.
export * from './schema';
export * from './sqlite-store';
